# Scoped implementation

Read repository guidance and the relevant memory-bank entries first. Inspect the affected code and current diff before editing.

Implement only the requested outcome using established repository patterns. Do not invent infrastructure or make unrelated cleanup changes. Protect secrets and redact sensitive data.

Before finishing, run the smallest relevant checks. Report changed files, behavior, commands and results, assumptions, and any verification gaps. Record durable decisions in the memory bank when its workflow calls for it.
